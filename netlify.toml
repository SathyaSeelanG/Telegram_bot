[build]
  functions = "api"  # Path to your functions directory
  publish = "public"  # This can be omitted if not using a static site

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200
  [build]
  functions = "api"  # Path to your functions directory
  publish = "public"  # Optional, if not using static files
  command = "npm install"  # Add this line to ensure dependencies are installed

